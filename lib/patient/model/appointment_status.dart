/// IMPORTANT: Do not change the order, it actually matters. The patient page status sorting is done via enum index!
enum AppointmentStatus {
  recentlyFinished,
  current,
  next,
  upcoming,
  finished,
}
